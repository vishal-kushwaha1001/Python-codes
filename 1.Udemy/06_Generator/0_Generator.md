# Generator
 ⇒ A generator is a special type of function/iterator
       that produces values one at a time, instead of creating and 
       storing all values in memory at once.

    ⇒ The main keyword used is yield.